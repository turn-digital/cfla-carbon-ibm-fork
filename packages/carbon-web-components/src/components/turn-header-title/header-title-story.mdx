import { Props, Description } from '@storybook/addon-docs/blocks';
import { cdnJs, cdnCss } from '../../globals/internal/storybook-cdn';

# Header title

### JS (via import)

```imoprt
<script type="module" src="https://demo.turn.lv/cfla_dist/turn-header-title.min.js"></script>
```

### HTML

```html
<cds-header-title title="Proposed Header Title"> </cds-header-title>
```

## `<cds-header-title>` attributes and properties

<Props of="cds-header-title" />
