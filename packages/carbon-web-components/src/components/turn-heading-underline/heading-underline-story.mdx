import { Props, Description } from '@storybook/addon-docs/blocks';
import { cdnJs, cdnCss } from '../../globals/internal/storybook-cdn';

# Label underline

## Getting started

### JS (via import)

```imoprt
<script type="module" src="https://demo.turn.lv/cfla_dist/turn-heading-underline.min.js"></script>
```

### HTML

```JS
 const counterArr = [
    { type: 'red', count: 2 },
    { type: 'green', count: 1 },
  ];
```

```html
<cds-heading-underline label="Apraksts" .counterArr="${counterArr}">
</cds-heading-underline>
```

## `<cds-heading-underline>` attributes and properties

<Props of="cds-heading-underline" />
