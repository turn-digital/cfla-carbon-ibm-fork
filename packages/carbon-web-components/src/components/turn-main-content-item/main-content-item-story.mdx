import { Props, Description } from '@storybook/addon-docs/blocks';
import { cdnJs, cdnCss } from '../../globals/internal/storybook-cdn';

# Main content item
## Apraksts
Komponente - ietvars. Laukums, kurā tiek ievietotas citas komponentes.

## Anatomija
Ietvars bez sava satura, kurā ievietot citas komponentes

## Dizaina un UX komentāri
Laukums, kurā tiek ievietotas citas komponentes.

## Getting started
### JS (via import)

```imoprt
<script type="module" src="https://demo.turn.lv/cfla_dist/turn-main-content-item.min.js"></script>
```

### HTML

```html
<cds-main-content-item>Some content inside</cds-main-content-item>
```

## `<cds-main-content-item>` attributes and properties

<Props of="cds-main-content-item" />
