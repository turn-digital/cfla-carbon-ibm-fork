import { Props, Description } from '@storybook/addon-docs/blocks';
import { cdnJs, cdnCss } from '../../globals/internal/storybook-cdn';

# Diff checker button

type: added, edited, deleted

## Getting started

### JS (via import)

```imoprt
<script type="module" src="https://demo.turn.lv/cfla_dist/turn-diff-checker-btn.min.js"></script>
```

### HTML

```JS

```

```html
    <cds-diff-checker-btn
      type="added"
      date="2021-02-02">
    </cds-diff-checker-btn>
```

## `<cds-diff-checker-btn>` attributes and properties

<Props of="cds-diff-checker-btn" />
